server {
    listen 8080;

    # Docker's embedded DNS tracks recreated service containers. Using a variable makes nginx
    # resolve `api` per DNS TTL instead of pinning the IP that existed when nginx started.
    resolver 127.0.0.11 valid=10s ipv6=off;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — React Router handles client-side routing
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy all /api calls to the NestJS api container
    location /api {
        set                $api_upstream http://api:3000;
        proxy_pass         $api_upstream;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }

    # Lightweight health probe for the Docker HEALTHCHECK in Dockerfile.client
    location /health.txt {
        access_log off;
        return 200 "ok";
        add_header Content-Type text/plain;
    }
}
